# Import the random module here
import random
# Split string method
names_string = input("Give me everybody's names, separated by a comma. ")
names = names_string.split(", ")
size=len(names)
Bill= random.randint(0,size)
print(f"{names[Bill]} is going to buy the meal today!" )
